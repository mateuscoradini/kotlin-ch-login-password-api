server:
  port: 0

management:
  endpoints:
    web:
      exposure:
        include: health

springdoc:
  api-docs:
    enabled: false
  swagger-ui:
    enabled: false

logging:
  level:
    root: ERROR
    br.com.coradini.kotlin.ch.login.api: ERROR

